interface:
  display_name: "PR Feedback Loop"
  short_description: "Run a Copilot review and CI feedback loop"
  default_prompt: "Use $pr-feedback-loop to drive this pull request through Copilot review and CI feedback until comments and fixable checks are resolved."
policy:
  allow_implicit_invocation: false
